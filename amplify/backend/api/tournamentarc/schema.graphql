type Login @model @auth(rules: [{allow: public}]) {
  id: ID!
  password: String
}
 
"""Tournament Event Table model object, represents a Tournament Event
includes below attributes
##Relationships to be added later
"""
type TournamentEvent @model {
  id: ID!
  eventName: String
  description: String
  organizingBody: String
  location: String
  startDate: AWSDate
  endDate: AWSDate
  totalPrizeMoney: Float
}